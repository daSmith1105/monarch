dojo.provide( "rda.util.Dimension" );

dojo.declare(
	"rda.util.Dimension",
	null,
	{
		width: 0,
		height: 0,

		constructor: function( bWidth, bHeight )
		{
			this.width = bWidth;
			this.height = bHeight;
		}

	}
);
